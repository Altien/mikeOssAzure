{
  "$schema": "https://aka.ms/bicep/config",
  "//": [
    "Strict-mode linter config for the marketplace template. The rules",
    "promoted to 'error' below cover the classes of issue that rejected",
    "1.0.2 at Partner Center, so a `bicep build` failure now catches them",
    "before they reach scripts/preflight/arm-ttk.ps1 - one feedback loop",
    "earlier and per-file rather than per-package.",
    "",
    "Promoted to error:",
    "  outputs-should-not-contain-secrets - the literal Phase 0 rejection.",
    "  use-recent-api-versions            - the @2023-07-01 KV secrets issue.",
    "  secure-params-in-nested-deploy     - secure values must flow through",
    "                                       inner-scope nested deployments.",
    "",
    "Kept at warning:",
    "  no-unused-params - useful nudge but ships warning, not error.",
    "",
    "All other rules left at their Bicep-shipped defaults."
  ],
  "analyzers": {
    "core": {
      "rules": {
        "outputs-should-not-contain-secrets": { "level": "error" },
        "use-recent-api-versions":            { "level": "error" },
        "secure-params-in-nested-deploy":     { "level": "error" },
        "no-unused-params":                   { "level": "warning" }
      }
    }
  }
}
